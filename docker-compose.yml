version: '3.8'
services:
  frontend:
    image: hoangle2004/my-frontend:latest # Image FE bạn vừa viết Dockerfile ở câu trước
    container_name: seft_note_frontend
    restart: always
    ports:
      - "80:80"
    environment:
      - VITE_API_URL=${VITE_API_URL}